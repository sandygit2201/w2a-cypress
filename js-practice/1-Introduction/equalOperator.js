let num1 = 10
let num2 = 10
//  == compare only data 
// === compare data with data type
if (num1 === num2) {
    console.log("Both num1, num2 are same ")
} else {
    console.log("Both num1, num2 are not same ")
}
